spring:
  jpa:
    hibernate.ddl-auto: validate
    generate-ddl: true
    show-sql: false
    database-platform: org.hibernate.dialect.MySQL8Dialect

  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/formicary?characterEncoding=utf-8&useUnicode=true
    username: root
    password: Aa123456
  application:
    name: Formicary REST
